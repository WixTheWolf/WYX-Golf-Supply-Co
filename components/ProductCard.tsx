import Image from 'next/image';import Link from 'next/link';import type {Product} from '@/types/shopify';import {money} from '@/lib/demo';
export function ProductCard({product}:{product:Product}){const img=product.featuredImage;return <article className="product-card"><Link href={`/products/${product.handle}`} className="product-image-link">{img&&<Image src={img.url} alt={img.altText||product.title} width={900} height={675}/>}</Link><div className="product-card-body"><span className="badge">{product.tags?.[0]||product.productType||'Core Collection'}</span><h3>{product.title}</h3><p>{product.description}</p><div className="product-card-footer"><span className="price">{money(product.priceRange.minVariantPrice)}</span><Link className="text-link" href={`/products/${product.handle}`}>View Product</Link></div></div></article>}
