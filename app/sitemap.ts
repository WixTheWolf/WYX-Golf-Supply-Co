import type {MetadataRoute} from 'next';import {demoProducts} from '@/lib/demo';import {posts} from '@/lib/journal';export default function sitemap():MetadataRoute.Sitemap{const base='https://wyx-golf-supply-co.vercel.app';return['','/products','/story','/journal','/cart','/privacy','/shipping-returns','/contact',...demoProducts.map(p=>`/products/${p.handle}`),...posts.map(p=>`/journal/${p.slug}`)].map(url=>({url:base+url,lastModified:new Date()}))}
